class firstClass # comment
{
    def  main( ): int{
        while( good && i < data.length ){
            good = data[i] ;
            i = i + 1;
            if (!x == 1) then 
                j = 1;
            else {
                j = 2; 
                {
                    k = 1;
                }
            }
        }
        return true;
    }

}
class smlExample extends exl
    {
        def main( args :string) :boolean #test*&* 
        {
            return arr[3+8];
        }
         def main2( args :string, arg2: int[]) :boolean #test*&* 
        {
            return arr[x+6*s+1];
        }

    }

