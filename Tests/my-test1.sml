class b {
    def x() : int {

    }
}

class a extends b {
    var x : int;
    var x : int[];
    var x : boolean;
    var x : string;

    def x(a1: int, a2: boolean, a3: string, a4: int[]) : string {
        var x : int;
        var x : int[];
        var x : boolean;
        var x : string;

        if(
            !x == 1 ||
            x[x*2 + 5*x] == true &&
            a+b+c+d /d/ c +d ||
            a / b * c + d - e ||
            a > b ||
            b < d
        ) then {
            writeln(hello.class2.main((2 * (((3))) + x)));
        }

        while(a[34 + 4 + 3 + 3 ]) {
            a = a + 1;
        }
    }
}