class Test {
	def main() : int {
		writeln(new int[10].length);
		return 0;
	}
}

class Book {
	def main (f1: int, main: string) : int[] {
		y = a.b(); 
		z = new a().n(x, y);
		x = (a.n()).c();
		l = ((a.n()).c()).v();
		t = ((a()).x()).l();
		u = ((new a()).x()).l();
		return a.b().c();
	}
}