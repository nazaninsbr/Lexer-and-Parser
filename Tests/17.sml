class Test {
	def main() : int {
		writeln(new int[10].length);
		return 0;
	}
}

class Book {
	def main (f1: int, main: string) : int[] {
		x = ((a.n()).c()).v();
		return a.b().c();
	}

	def f(): string {
		return (B||C) + (m*9) - 3;
	}
}